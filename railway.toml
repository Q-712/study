[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 2"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyDelay = 5

[variables]
FLASK_ENV = "production"
